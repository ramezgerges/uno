<Page
    x:Class="UITests.Windows_UI_Xaml_Controls.RoundedWebGpu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="White">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Spacing="40">
        <!-- green child fills the border; corners must clip to the rounded shape -->
        <Border Width="180" Height="180" CornerRadius="45">
            <Grid Background="Green" />
        </Border>
        <!-- a shadowed element forces the whole frame through the SEGMENTED renderer -->
        <Border x:Name="Shadowed" Width="120" Height="120" Background="Orange" CornerRadius="12" />
    </StackPanel>
</Page>
